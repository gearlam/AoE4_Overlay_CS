<UserControl x:Class="AoE4OverlayCS.Views.GamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Background="#F0F0F0" Height="40">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/> <!-- Team 1 -->
                <ColumnDefinition Width="2*"/> <!-- Team 2 -->
                <ColumnDefinition Width="100"/> <!-- Map -->
                <ColumnDefinition Width="120"/> <!-- Started -->
                <ColumnDefinition Width="80"/>  <!-- Mode -->
                <ColumnDefinition Width="60"/>  <!-- Result -->
                <ColumnDefinition Width="60"/>  <!-- Diff -->
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="{DynamicResource Games_Header_Team1}" Grid.Column="0" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Games_Header_Team2}" Grid.Column="1" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Games_Header_Map}" Grid.Column="2" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Games_Header_Started}" Grid.Column="3" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Games_Header_Mode}" Grid.Column="4" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Games_Header_Result}" Grid.Column="5" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="{DynamicResource Games_Header_Diff}" Grid.Column="6" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        
        <!-- List -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Games}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/> <!-- Team 1 -->
                                    <ColumnDefinition Width="2*"/> <!-- Team 2 -->
                                    <ColumnDefinition Width="100"/> <!-- Map -->
                                    <ColumnDefinition Width="120"/> <!-- Started -->
                                    <ColumnDefinition Width="80"/>  <!-- Mode -->
                                    <ColumnDefinition Width="60"/>  <!-- Result -->
                                    <ColumnDefinition Width="60"/>  <!-- Diff -->
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="{Binding Team1Display}" Grid.Column="0" TextWrapping="Wrap" FontSize="11" />
                                <TextBlock Text="{Binding Team2Display}" Grid.Column="1" TextWrapping="Wrap" FontSize="11" />

                                <TextBlock Text="{Binding Map}" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" FontSize="11"/>
                                <TextBlock Text="{Binding Started}" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                                <TextBlock Text="{Binding Mode}" Grid.Column="4" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11"/>
                                <TextBlock Text="{Binding Result}" Grid.Column="5" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding RatingDiff}" Grid.Column="6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
