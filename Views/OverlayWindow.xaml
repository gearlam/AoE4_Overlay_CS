<Window x:Class="AoE4OverlayCS.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AoE IV: Overlay" Height="400" Width="700"
    WindowStyle="None" AllowsTransparency="True" Background="Transparent"
    Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
    MouseLeftButtonDown="Window_MouseLeftButtonDown"
    Icon="pack://siteoforigin:,,,/img/aoe4_sword_shield.ico">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid>
        <!-- Content Panel -->
        <Grid Margin="6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="MapLabel" Text="" FontSize="12" FontWeight="Bold" Foreground="#FFFF00" Margin="0,0,0,6" Effect="{DynamicResource ShadowEffect}"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="28"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel x:Name="TeamLeftPanel" Grid.Column="0"/>
                <StackPanel x:Name="TeamRightPanel" Grid.Column="2" HorizontalAlignment="Right"/>
            </Grid>
        </Grid>
        
        <!-- Unlock UI: Border and Resize Grip -->
        <Border x:Name="UnlockBorder" BorderBrush="Yellow" BorderThickness="2" Visibility="Collapsed" IsHitTestVisible="False"/>
        <ResizeGrip x:Name="ResizeGripControl" HorizontalAlignment="Right" VerticalAlignment="Bottom" Visibility="Collapsed" Cursor="SizeNWSE" 
                    MouseLeftButtonDown="ResizeGrip_MouseLeftButtonDown"/>
    </Grid>
</Window>
